# Emacs Dotfiles

Configurația mea personală de Emacs.

## Conținut
- `early-init.el` + `init.el`
- Module organizate în `lisp/`
- Folosește `straight.el` + `use-package`

## Instalare

```bash
git clone https://github.com/Ianiksdf/Emacs-Dotfiles.git ~/.config/emacs
  * [ ] 

Test
